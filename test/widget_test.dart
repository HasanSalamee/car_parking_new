/*import 'package:car_parking/main.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App starts on LoginScreen if initialRoute is /login',
      (WidgetTester tester) async {
    await tester.pumpWidget(const MyApp(initialRoute: '/login'));
    await tester.pumpAndSettle();

    // تحقق من وجود نص مميز في شاشة تسجيل الدخول
    expect(find.text('تسجيل الدخول'), findsOneWidget);
  });

  testWidgets('App starts on HomeScreen if initialRoute is /home',
      (WidgetTester tester) async {
    await tester.pumpWidget(const MyApp(initialRoute: '/home'));
    await tester.pumpAndSettle();

    // تحقق من وجود نص مميز في الصفحة الرئيسية
    expect(find.text('الرئيسية'), findsOneWidget);
  });
}
*/
